<!DOCTYPE html>
<html>
    <head>
        {% include '/includes/head.html.twig' %}
        <meta charset="utf-8">
        <title>Book | Edit</title>
    </head>
    <body>
        <h1>Edit {{ book.getTitle }}</h1>

        <form action="/book/{{ book.getId }}" method="post">
            <input name="_method" type="hidden" value="patch">

            <label for="new_title">New Title:</label>
            <input type="text" name="new_title" value="{{ book.getTitle }}">

            <label for="new_copies">Number of copies:</label>
            <input type="text" name="new_copies" value="{{ book.getCopies}}">

            <button type="submit" class="btn btn-primary">Update Book</button>
        </form>

        <a href="/">Home</a>
    </body>
</html>
