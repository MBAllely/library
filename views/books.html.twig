<!DOCTYPE html>
<html>
    <head>
        {% include '/includes/head.html.twig' %}
        <meta charset="utf-8">
        <title>Library | Books</title>
    </head>
    <body>
        <div class="container">
            <h2>Books!</h2>

            <h3>Here are the books in the current catalog:</h3>
            <ul>
                {% if books is not empty %}
                    {% for book in books %}
                        <h5><a href="/book/{{ book.getId }}">{{ book.getTitle }}</a></h5>
                    {% endfor %}
                {% else %}
                    <h1>There are no books!  Support your local library, asshole!</h1>
                {% endif %}
            </ul>

            <h5>Add a book to the database:</h5>

            <form action="/books" method="post">
                <label for="title">Title:</label>
                <input type="text" name="title" value="">

                <label for="copies">Copies:</label>
                <input type="text" name="copies" value="">
                <button type="submit" class="btn btn-primary">Add Book</button>
            </form>

            <a href="/">Home</a>

        </div>
    </body>
</html>
