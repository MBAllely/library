<!DOCTYPE html>
<html>
    <head>
        {% include '/includes/head.html.twig' %}
        <meta charset="utf-8">
        <title>Patron | Edit</title>
    </head>
    <body>
        <h1>Edit {{ patron.getPatronName }}'s profile</h1>

        <form action="/patron/{{ patron.getId }}" method="post">
            <input name="_method" type="hidden" value="patch">
            <label for="new_patron_name">New Name:</label>
            <input type="text" name="new_patron_name" value="{{ patron.getPatronName }}">

            <button type="submit" class="btn btn-primary">Update patron</button>

        </form>

        <a href="/">Home</a>
    </body>
</html>
